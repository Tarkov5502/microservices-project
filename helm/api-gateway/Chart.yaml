apiVersion: v2
name: api-gateway
description: |
  Helm chart for the API Gateway microservice.
  Helm is the Kubernetes package manager — it templates K8s YAML with values
  so the same chart deploys identically to dev, staging, and prod by just
  changing values.yaml (or passing --set flags).
type: application
version: 0.1.0
appVersion: "1.0.0"
