fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pyjwt==2.8.0
pydantic-settings==2.2.1
prometheus-client==0.20.0
# hiredis: C extension for the Redis protocol parser — ~10x faster than the
# pure-Python implementation for high-throughput rate-limit checks.
redis[hiredis]==5.0.4
