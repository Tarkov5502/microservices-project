# requirements-dev.txt — test and development dependencies.
# NOT installed in the production Docker image (Dockerfile only copies requirements.txt).
# Install locally: pip install -r requirements-dev.txt
#
# pytest-asyncio: required for @pytest.mark.asyncio and asyncio_mode="auto"
# httpx: async HTTP client used by FastAPI's TestClient and in gateway tests
# respx: mock library for httpx — intercept outbound HTTP calls in tests

pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0
